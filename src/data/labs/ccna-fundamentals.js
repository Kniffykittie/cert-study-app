const set = {
  id: 'ccna-fundamentals',
  title: 'CCNA Fundamentals',
  cert: 'ccna',
  color: '#0080FF',
  cssColor: 'var(--accent-blue)',
  description: 'Eight hands-on Packet Tracer labs covering the core skills tested on the CCNA 200-301. Each lab builds directly on the last — complete them in order.',
  software: 'Cisco Packet Tracer 8.x (free at netacad.com)',
  tip: 'Lab 1 is the foundation everything else depends on. Take your time with the Router-on-a-Stick subinterface config — getting encapsulation dot1Q and IP addressing right here saves headaches in every later lab.',
  labs: [
    {
      id: 'lab-1-vlans',
      number: 1,
      title: 'VLANs & Router-on-a-Stick',
      duration: '45–60 min',
      difficulty: 'beginner',
      domains: ['2.0 Network Access'],
      description: 'Configure VLANs on a switch, assign access ports, set up a trunk to the router, and enable inter-VLAN routing using subinterfaces (Router-on-a-Stick).',
      objectives: [
        'Create VLANs 10, 20, and 30 on a Catalyst switch',
        'Assign switch ports to the correct VLANs',
        'Configure a trunk port between the switch and router',
        'Configure subinterfaces on the router with 802.1Q encapsulation',
        'Verify that PCs in different VLANs can communicate through the router',
      ],
      topology: {
        viewBox: '0 0 800 420',
        nodes: [
          { id: 'r1', type: 'router', label: 'R1', sublabel: 'Cisco 1941', x: 400, y: 70 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: 'Catalyst 2960', x: 400, y: 220 },
          { id: 'pc1', type: 'pc', label: 'PC1', sublabel: 'VLAN 10\n10.1.10.2/24', x: 150, y: 370 },
          { id: 'pc2', type: 'pc', label: 'PC2', sublabel: 'VLAN 20\n10.1.20.2/24', x: 400, y: 370 },
          { id: 'pc3', type: 'pc', label: 'PC3', sublabel: 'VLAN 30\n10.1.30.2/24', x: 650, y: 370 },
        ],
        connections: [
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0', toLabel: 'G0/1 (trunk)', style: 'trunk' },
          { from: 'sw1', to: 'pc1', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc2', fromLabel: 'Fa0/2', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc3', fromLabel: 'Fa0/3', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Build the topology',
          description: 'In Packet Tracer, add one Cisco 1941 router, one Catalyst 2960 switch, and three PCs. Connect R1 G0/0 to SW1 G0/1 with a straight-through cable. Connect PC1 to Fa0/1, PC2 to Fa0/2, PC3 to Fa0/3.',
          commands: [],
          verify: '',
          expectedOutput: 'All link lights should turn green after a few seconds.',
          hints: [
            'Use Copper Straight-Through cables for all connections in this lab.',
            'In Packet Tracer, switch ports auto-negotiate — you don\'t need crossover cables between switch and PC.',
          ],
        },
        {
          id: 'step-2',
          title: 'Create VLANs on SW1',
          description: 'Create VLANs 10, 20, and 30 with descriptive names on SW1.',
          commands: [
            'SW1# conf t',
            'SW1(config)# vlan 10',
            'SW1(config-vlan)# name SALES',
            'SW1(config-vlan)# vlan 20',
            'SW1(config-vlan)# name HR',
            'SW1(config-vlan)# vlan 30',
            'SW1(config-vlan)# name IT',
            'SW1(config-vlan)# end',
          ],
          verify: 'SW1# show vlan brief',
          expectedOutput: 'VLANs 10, 20, and 30 should appear as active with their names. They will show no ports assigned yet.',
          hints: [
            'Enter global config mode with conf t before issuing vlan commands.',
            'You must save VLANs with end or exit after creating them or they may not persist.',
            'The name command is optional but a real-world best practice — examiners sometimes test it.',
          ],
        },
        {
          id: 'step-3',
          title: 'Assign access ports',
          description: 'Configure Fa0/1 as access VLAN 10, Fa0/2 as access VLAN 20, and Fa0/3 as access VLAN 30.',
          commands: [
            'SW1(config)# interface fa0/1',
            'SW1(config-if)# switchport mode access',
            'SW1(config-if)# switchport access vlan 10',
            'SW1(config-if)# interface fa0/2',
            'SW1(config-if)# switchport mode access',
            'SW1(config-if)# switchport access vlan 20',
            'SW1(config-if)# interface fa0/3',
            'SW1(config-if)# switchport mode access',
            'SW1(config-if)# switchport access vlan 30',
          ],
          verify: 'SW1# show vlan brief',
          expectedOutput: 'Each VLAN should now show the correct port in the Ports column. VLAN 10: Fa0/1, VLAN 20: Fa0/2, VLAN 30: Fa0/3.',
          hints: [
            'switchport mode access must be set before switchport access vlan — otherwise the port may not accept the VLAN assignment.',
            'You can use "interface range fa0/1 - 3" to configure multiple ports, but you\'d need separate commands per VLAN so doing them individually is cleaner here.',
          ],
        },
        {
          id: 'step-4',
          title: 'Configure the trunk port',
          description: 'Configure G0/1 (uplink to router) as a trunk port carrying VLANs 10, 20, and 30.',
          commands: [
            'SW1(config)# interface g0/1',
            'SW1(config-if)# switchport mode trunk',
            'SW1(config-if)# switchport trunk allowed vlan 10,20,30',
          ],
          verify: 'SW1# show interfaces trunk',
          expectedOutput: 'G0/1 should appear in trunking mode with VLANs 10, 20, 30 in the allowed and active columns.',
          hints: [
            'On a real 2960, you may need "switchport trunk encapsulation dot1q" before setting trunk mode. Packet Tracer handles this automatically.',
            'If the trunk does not come up, check that the router interface is enabled — it defaults to shutdown.',
          ],
        },
        {
          id: 'step-5',
          title: 'Configure Router-on-a-Stick subinterfaces',
          description: 'Enable G0/0 on R1, then create three subinterfaces — one per VLAN — with 802.1Q encapsulation and gateway IPs.',
          commands: [
            'R1(config)# interface g0/0',
            'R1(config-if)# no shutdown',
            'R1(config-if)# interface g0/0.10',
            'R1(config-subif)# encapsulation dot1Q 10',
            'R1(config-subif)# ip address 10.1.10.1 255.255.255.0',
            'R1(config-subif)# interface g0/0.20',
            'R1(config-subif)# encapsulation dot1Q 20',
            'R1(config-subif)# ip address 10.1.20.1 255.255.255.0',
            'R1(config-subif)# interface g0/0.30',
            'R1(config-subif)# encapsulation dot1Q 30',
            'R1(config-subif)# ip address 10.1.30.1 255.255.255.0',
          ],
          verify: 'R1# show ip interface brief',
          expectedOutput: 'G0/0, G0/0.10, G0/0.20, and G0/0.30 should all show up/up with their assigned IPs.',
          hints: [
            'The physical interface G0/0 must be "no shutdown" — the subinterfaces inherit its state.',
            'encapsulation dot1Q must come before the ip address command on each subinterface.',
            'The VLAN number in "encapsulation dot1Q 10" must exactly match the VLAN on the switch.',
          ],
        },
        {
          id: 'step-6',
          title: 'Configure PC IP addresses',
          description: 'Set static IPs, subnet masks, and default gateways on all three PCs.',
          commands: [
            'PC1: IP 10.1.10.2 / Mask 255.255.255.0 / GW 10.1.10.1',
            'PC2: IP 10.1.20.2 / Mask 255.255.255.0 / GW 10.1.20.1',
            'PC3: IP 10.1.30.2 / Mask 255.255.255.0 / GW 10.1.30.1',
          ],
          verify: 'Click a PC → Desktop → IP Configuration',
          expectedOutput: 'Each PC should show its IP, mask, and gateway correctly set.',
          hints: [
            'In Packet Tracer, click the PC → Desktop tab → IP Configuration to set addresses manually.',
            'The gateway must be the subinterface IP on R1 for that VLAN — not the physical interface.',
          ],
        },
        {
          id: 'step-7',
          title: 'Verify inter-VLAN routing',
          description: 'Ping from PC1 to PC2 and PC3. All pings should succeed, proving traffic is routing through R1.',
          commands: [
            'PC1> ping 10.1.20.2',
            'PC1> ping 10.1.30.2',
          ],
          verify: 'PC1 Desktop → Command Prompt → ping 10.1.20.2',
          expectedOutput: 'Both pings should return 4/4 replies. First ping may lose 1 packet due to ARP.',
          hints: [
            'If pings fail, first ping within the same VLAN (PC1 to 10.1.10.1 gateway) to isolate the problem.',
            'Check the trunk — run "show interfaces trunk" on SW1 and confirm G0/1 is active.',
            'Verify the encapsulation dot1Q numbers match the VLAN IDs exactly.',
          ],
        },
      ],
      tips: [
        'Router-on-a-Stick is called that because all traffic goes up and down the same physical link — it\'s a bottleneck in production but the standard way to test inter-VLAN routing on the CCNA.',
        'The encapsulation dot1Q command is the most common mistake — it must come before the ip address on every subinterface.',
        'Keep your IP scheme logical: 10.1.VLAN.x (e.g. VLAN 10 = 10.1.10.x). Makes troubleshooting much faster.',
        'ACLs in Lab 4 will build directly on this topology — save your Packet Tracer file.',
      ],
    },

    {
      id: 'lab-2-dhcp',
      number: 2,
      title: 'DHCP & IP Helper Address',
      duration: '30–45 min',
      difficulty: 'beginner',
      domains: ['1.0 Network Fundamentals', '2.0 Network Access'],
      description: 'Configure DHCP pools on R1 so PCs receive IPs automatically. Understand excluded addresses and the ip helper-address concept for when DHCP lives on a separate server.',
      objectives: [
        'Configure DHCP excluded address ranges on R1',
        'Create DHCP pools for VLANs 10, 20, and 30',
        'Verify PCs receive IPs, masks, and gateways dynamically',
        'Understand when and why ip helper-address is used',
      ],
      topology: {
        viewBox: '0 0 800 420',
        nodes: [
          { id: 'r1', type: 'router', label: 'R1', sublabel: 'DHCP Server\n(built-in)', x: 400, y: 70 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: 'Catalyst 2960', x: 400, y: 220 },
          { id: 'pc1', type: 'pc', label: 'PC1', sublabel: 'VLAN 10\nDHCP', x: 150, y: 370 },
          { id: 'pc2', type: 'pc', label: 'PC2', sublabel: 'VLAN 20\nDHCP', x: 400, y: 370 },
          { id: 'pc3', type: 'pc', label: 'PC3', sublabel: 'VLAN 30\nDHCP', x: 650, y: 370 },
        ],
        connections: [
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0', toLabel: 'G0/1 (trunk)', style: 'trunk' },
          { from: 'sw1', to: 'pc1', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc2', fromLabel: 'Fa0/2', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc3', fromLabel: 'Fa0/3', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Load your Lab 1 topology',
          description: 'Open your saved Lab 1 file. The VLANs, trunk, subinterfaces, and PC connections should all already be configured. Change all PC IP configurations from Static to DHCP.',
          commands: [],
          verify: 'Click each PC → Desktop → IP Configuration → select DHCP',
          expectedOutput: 'PCs will show 0.0.0.0 until the DHCP pools are configured on R1.',
          hints: [
            'If you don\'t have a Lab 1 file, redo the topology and subinterface config from Lab 1 before continuing.',
          ],
        },
        {
          id: 'step-2',
          title: 'Exclude static addresses from DHCP pools',
          description: 'Reserve the first 10 IPs in each subnet for static assignment (routers, servers, etc.) so DHCP doesn\'t hand them out.',
          commands: [
            'R1(config)# ip dhcp excluded-address 10.1.10.1 10.1.10.10',
            'R1(config)# ip dhcp excluded-address 10.1.20.1 10.1.20.10',
            'R1(config)# ip dhcp excluded-address 10.1.30.1 10.1.30.10',
          ],
          verify: 'R1# show running-config | include excluded',
          expectedOutput: 'Three excluded-address lines should appear in the running config.',
          hints: [
            'Always exclude your router gateway IPs first — if a PC accidentally got 10.1.10.1 it would conflict with the router subinterface.',
            'Excluded ranges are inclusive on both ends.',
          ],
        },
        {
          id: 'step-3',
          title: 'Create DHCP pools',
          description: 'Create one DHCP pool per VLAN, specifying the network, default gateway, and DNS server.',
          commands: [
            'R1(config)# ip dhcp pool VLAN10',
            'R1(dhcp-config)# network 10.1.10.0 255.255.255.0',
            'R1(dhcp-config)# default-router 10.1.10.1',
            'R1(dhcp-config)# dns-server 8.8.8.8',
            'R1(dhcp-config)# ip dhcp pool VLAN20',
            'R1(dhcp-config)# network 10.1.20.0 255.255.255.0',
            'R1(dhcp-config)# default-router 10.1.20.1',
            'R1(dhcp-config)# dns-server 8.8.8.8',
            'R1(dhcp-config)# ip dhcp pool VLAN30',
            'R1(dhcp-config)# network 10.1.30.0 255.255.255.0',
            'R1(dhcp-config)# default-router 10.1.30.1',
            'R1(dhcp-config)# dns-server 8.8.8.8',
          ],
          verify: 'R1# show ip dhcp pool',
          expectedOutput: 'Three pools listed, each showing their network range and utilization.',
          hints: [
            'The pool name (VLAN10, VLAN20, VLAN30) is local only — it doesn\'t need to match anything on the switch.',
            'default-router must match the subinterface IP on R1 for that VLAN.',
          ],
        },
        {
          id: 'step-4',
          title: 'Verify PCs receive addresses',
          description: 'Check that all three PCs have been assigned IPs from their correct pools.',
          commands: [
            'PC1> ipconfig',
          ],
          verify: 'Click each PC → Desktop → IP Configuration or open Command Prompt and run ipconfig',
          expectedOutput: 'PC1 should have 10.1.10.11+, PC2 should have 10.1.20.11+, PC3 should have 10.1.30.11+. Each should have the correct gateway and DNS.',
          hints: [
            'If a PC still shows 0.0.0.0, click "Fast Forward" in Packet Tracer to skip the DHCP negotiation timer.',
            'Run "show ip dhcp binding" on R1 to see all active leases.',
          ],
        },
        {
          id: 'step-5',
          title: 'Verify DHCP bindings on R1',
          description: 'Confirm R1 is tracking all three leases.',
          commands: [
            'R1# show ip dhcp binding',
            'R1# show ip dhcp pool',
          ],
          verify: 'R1# show ip dhcp binding',
          expectedOutput: 'Three entries — one per PC — showing MAC address, assigned IP, and lease expiry.',
          hints: [
            '"show ip dhcp conflict" is useful if a PC can\'t get an address — it shows IPs that caused conflicts.',
          ],
        },
        {
          id: 'step-6',
          title: 'Test connectivity across VLANs',
          description: 'Ping from PC1 to PC2 and PC3 to confirm routing still works with dynamically assigned IPs.',
          commands: [
            'PC1> ping 10.1.20.x  (use the IP shown on PC2)',
            'PC1> ping 10.1.30.x  (use the IP shown on PC3)',
          ],
          verify: 'Command Prompt on PC1',
          expectedOutput: '4/4 replies from both PCs.',
          hints: [
            'DHCP addresses are dynamic — look up PC2 and PC3\'s current IPs before pinging.',
          ],
        },
      ],
      tips: [
        'The ip helper-address command is needed when your DHCP server is on a different subnet from the clients. You\'d put it on the router interface facing the clients, pointing to the server IP. It\'s not needed here since R1 IS the DHCP server, but it appears on CCNA exams frequently.',
        '"show ip dhcp binding" is your best friend for verifying DHCP in production and on the exam.',
        'Always exclude at minimum your gateway IPs plus a few extra for static devices before creating pools.',
      ],
    },

    {
      id: 'lab-3-stp',
      number: 3,
      title: 'Spanning Tree & Redundancy',
      duration: '45–60 min',
      difficulty: 'intermediate',
      domains: ['2.0 Network Access'],
      description: 'Add a second switch to create redundant links, observe a broadcast storm, then watch STP prevent it. Configure PortFast and BPDU Guard on access ports.',
      objectives: [
        'Add a redundant switch link and observe the resulting broadcast storm',
        'Verify STP is blocking the redundant port to prevent loops',
        'Manipulate bridge priority to control which switch becomes root',
        'Configure PortFast and BPDU Guard on access ports',
      ],
      topology: {
        viewBox: '0 0 800 440',
        nodes: [
          { id: 'r1', type: 'router', label: 'R1', sublabel: 'Gateway', x: 400, y: 55 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: 'Root Bridge\n(priority 4096)', x: 220, y: 200 },
          { id: 'sw2', type: 'switch', label: 'SW2', sublabel: 'Catalyst 2960', x: 580, y: 200 },
          { id: 'pc1', type: 'pc', label: 'PC1', sublabel: 'VLAN 10', x: 100, y: 370 },
          { id: 'pc2', type: 'pc', label: 'PC2', sublabel: 'VLAN 20', x: 300, y: 370 },
          { id: 'pc3', type: 'pc', label: 'PC3', sublabel: 'VLAN 10', x: 500, y: 370 },
          { id: 'pc4', type: 'pc', label: 'PC4', sublabel: 'VLAN 20', x: 700, y: 370 },
        ],
        connections: [
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0', toLabel: 'G0/1', style: 'trunk' },
          { from: 'r1', to: 'sw2', fromLabel: 'G0/1', toLabel: 'G0/1', style: 'trunk' },
          { from: 'sw1', to: 'sw2', fromLabel: 'G0/2', toLabel: 'G0/2', style: 'redundant' },
          { from: 'sw1', to: 'pc1', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc2', fromLabel: 'Fa0/2', toLabel: '', style: 'access' },
          { from: 'sw2', to: 'pc3', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
          { from: 'sw2', to: 'pc4', fromLabel: 'Fa0/2', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Build the two-switch topology',
          description: 'Add a second Catalyst 2960 (SW2). Connect R1 G0/1 to SW2 G0/1. Connect PC3 to SW2 Fa0/1 (VLAN 10) and PC4 to SW2 Fa0/2 (VLAN 20). Do NOT connect SW1 to SW2 yet.',
          commands: [],
          verify: '',
          expectedOutput: 'Both switches are connected to R1. PCs on SW2 can communicate with the router.',
          hints: [
            'Use the same VLAN and trunk config from Lab 1 on SW2.',
            'R1 needs subinterfaces for VLAN 10 and 20 on G0/1 as well — or use a Layer 3 switch approach. For simplicity, add G0/1.10 and G0/1.20 subinterfaces to R1.',
          ],
        },
        {
          id: 'step-2',
          title: 'Create the redundant link — observe the storm',
          description: 'Connect SW1 G0/2 to SW2 G0/2 with a crossover cable. Watch what happens to the link lights and simulation panel.',
          commands: [],
          verify: 'Watch the Packet Tracer simulation panel in Realtime mode',
          expectedOutput: 'You may notice the network becomes sluggish or link utilization spikes. In simulation mode you can see frames looping endlessly. STP will eventually converge and block one port.',
          hints: [
            'Use Packet Tracer\'s Simulation mode (bottom right) to slow down packet flow and actually watch the broadcast frames multiply.',
            'STP convergence takes 30–50 seconds by default. Be patient.',
          ],
        },
        {
          id: 'step-3',
          title: 'Verify STP blocked the redundant port',
          description: 'After convergence, check which port STP put into blocking state.',
          commands: [
            'SW1# show spanning-tree',
            'SW2# show spanning-tree',
          ],
          verify: 'SW1# show spanning-tree vlan 10',
          expectedOutput: 'One switch should show as Root Bridge. The G0/2 port on the non-root switch should show as BLK (blocking). The other ports should be FWD (forwarding).',
          hints: [
            'The switch with the lowest Bridge ID (priority + MAC) becomes root. With default settings that\'s usually the switch with the lowest MAC address.',
            'Port states: BLK = blocking (not forwarding data), LIS = listening, LRN = learning, FWD = forwarding.',
          ],
        },
        {
          id: 'step-4',
          title: 'Force SW1 to become root bridge',
          description: 'Set SW1 as the root bridge for all VLANs by lowering its bridge priority.',
          commands: [
            'SW1(config)# spanning-tree vlan 10 priority 4096',
            'SW1(config)# spanning-tree vlan 20 priority 4096',
          ],
          verify: 'SW1# show spanning-tree vlan 10',
          expectedOutput: 'SW1 should now show "This bridge is the root" in the output.',
          hints: [
            'STP priorities must be multiples of 4096. Valid values: 0, 4096, 8192, 12288... 61440.',
            'You can also use "spanning-tree vlan 10 root primary" as a shortcut — it automatically sets an appropriate lower priority.',
          ],
        },
        {
          id: 'step-5',
          title: 'Configure PortFast and BPDU Guard',
          description: 'Enable PortFast on all PC-facing ports on both switches so they skip the 30-second STP listening/learning delay. Add BPDU Guard to shut them down if a switch is accidentally connected.',
          commands: [
            'SW1(config)# interface range fa0/1 - 2',
            'SW1(config-if-range)# spanning-tree portfast',
            'SW1(config-if-range)# spanning-tree bpduguard enable',
            'SW2(config)# interface range fa0/1 - 2',
            'SW2(config-if-range)# spanning-tree portfast',
            'SW2(config-if-range)# spanning-tree bpduguard enable',
          ],
          verify: 'SW1# show spanning-tree interface fa0/1 detail',
          expectedOutput: 'PortFast and BPDU Guard enabled should both appear in the output.',
          hints: [
            'PortFast should ONLY be used on ports connected to end devices (PCs, servers, printers) — never on ports facing other switches.',
            'BPDU Guard automatically puts the port into err-disabled state if it receives a BPDU (meaning a switch was connected). Recover with "shutdown" then "no shutdown".',
          ],
        },
      ],
      tips: [
        'The broadcast storm moment is the best learning experience in this entire lab. Let it happen, watch it in simulation mode, then let STP fix it. That visualization sticks.',
        'PortFast eliminates the 30-second delay when a PC connects — critical in production so workstations don\'t time out waiting for network at boot.',
        'BPDU Guard is the safety net for PortFast. Never enable PortFast without BPDU Guard on access ports.',
        'STP port roles: Root Port (best path to root), Designated Port (forwarding on that segment), Blocked Port (preventing loops).',
      ],
    },

    {
      id: 'lab-4-acls',
      number: 4,
      title: 'Access Control Lists',
      duration: '60–75 min',
      difficulty: 'intermediate',
      domains: ['5.0 Security Fundamentals'],
      description: 'Write and apply standard and extended ACLs to control traffic between VLANs. Master ACL placement — the most common CCNA mistake.',
      objectives: [
        'Write a standard numbered ACL to block a specific host',
        'Write a named extended ACL to permit/deny specific protocols',
        'Apply ACLs in the correct direction (in vs out) on router subinterfaces',
        'Verify ACLs with show ip access-lists',
      ],
      topology: {
        viewBox: '0 0 800 420',
        nodes: [
          { id: 'r1', type: 'router', label: 'R1', sublabel: 'ACL enforcement\npoint', x: 400, y: 70 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: 'Catalyst 2960', x: 400, y: 220 },
          { id: 'pc1', type: 'pc', label: 'PC1', sublabel: 'VLAN 10\n10.1.10.2', x: 130, y: 370 },
          { id: 'pc2', type: 'pc', label: 'PC2', sublabel: 'VLAN 20\n10.1.20.2', x: 310, y: 370 },
          { id: 'pc3', type: 'pc', label: 'PC3', sublabel: 'VLAN 30\n10.1.30.2', x: 490, y: 370 },
          { id: 'srv', type: 'server', label: 'SRV1', sublabel: 'VLAN 40\n10.1.40.2\n(HTTP/FTP)', x: 670, y: 370 },
        ],
        connections: [
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0', toLabel: 'G0/1 (trunk)', style: 'trunk' },
          { from: 'sw1', to: 'pc1', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc2', fromLabel: 'Fa0/2', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc3', fromLabel: 'Fa0/3', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'srv', fromLabel: 'Fa0/4', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Extend the topology with a server VLAN',
          description: 'Add VLAN 40 to SW1. Connect a Server device to Fa0/4 as access VLAN 40. Add subinterface G0/0.40 on R1 (10.1.40.1/24). Set the server to IP 10.1.40.2 with gateway 10.1.40.1. Add HTTP and FTP services on the server.',
          commands: [
            'SW1(config)# vlan 40',
            'SW1(config-vlan)# name SERVERS',
            'SW1(config)# interface fa0/4',
            'SW1(config-if)# switchport mode access',
            'SW1(config-if)# switchport access vlan 40',
            'R1(config)# interface g0/0.40',
            'R1(config-subif)# encapsulation dot1Q 40',
            'R1(config-subif)# ip address 10.1.40.1 255.255.255.0',
          ],
          verify: 'Ping from PC1 to 10.1.40.2 — should succeed before any ACLs are applied',
          expectedOutput: 'All PCs can reach the server at 10.1.40.2.',
          hints: ['Click the Server → Services tab → enable HTTP and FTP.'],
        },
        {
          id: 'step-2',
          title: 'Standard ACL — block PC1 from reaching the server',
          description: 'Create standard ACL 10 that denies traffic from PC1 (10.1.10.2) and permits everything else. Apply it outbound on G0/0.40 (close to destination — standard ACL placement rule).',
          commands: [
            'R1(config)# access-list 10 deny host 10.1.10.2',
            'R1(config)# access-list 10 permit any',
            'R1(config)# interface g0/0.40',
            'R1(config-subif)# ip access-group 10 out',
          ],
          verify: 'R1# show ip access-lists',
          expectedOutput: 'ACL 10 with two entries. After testing, the deny entry should show match counts incrementing.',
          hints: [
            'Standard ACLs filter by source IP only — no destination, no protocol.',
            'Standard ACLs should be placed CLOSE TO THE DESTINATION. If placed close to source, it could block legitimate traffic to other destinations.',
            'There is an implicit "deny any" at the end of every ACL — without the "permit any" line, all other traffic would be blocked too.',
          ],
        },
        {
          id: 'step-3',
          title: 'Test the standard ACL',
          description: 'Verify PC1 cannot reach the server but PC2 and PC3 can.',
          commands: [
            'PC1> ping 10.1.40.2  (should FAIL)',
            'PC2> ping 10.1.40.2  (should SUCCEED)',
            'PC3> ping 10.1.40.2  (should SUCCEED)',
          ],
          verify: 'Command Prompt on each PC',
          expectedOutput: 'PC1 pings are blocked. PC2 and PC3 pings succeed.',
          hints: [
            'If PC1 can still reach the server, check that the ACL is applied outbound ("out") on G0/0.40, not inbound.',
            'Run "show ip access-lists" and check if the deny entry match count is increasing.',
          ],
        },
        {
          id: 'step-4',
          title: 'Extended ACL — allow HTTP only from VLAN 20',
          description: 'Create a named extended ACL that permits HTTP (port 80) from VLAN 20 to the server, denies all other traffic from VLAN 20 to the server, and permits everything else. Apply inbound on G0/0.20 (close to source — extended ACL rule).',
          commands: [
            'R1(config)# ip access-list extended VLAN20-TO-SERVER',
            'R1(config-ext-nacl)# permit tcp 10.1.20.0 0.0.0.255 host 10.1.40.2 eq 80',
            'R1(config-ext-nacl)# deny ip 10.1.20.0 0.0.0.255 host 10.1.40.2',
            'R1(config-ext-nacl)# permit ip any any',
            'R1(config)# interface g0/0.20',
            'R1(config-subif)# ip access-group VLAN20-TO-SERVER in',
          ],
          verify: 'R1# show ip access-lists VLAN20-TO-SERVER',
          expectedOutput: 'Three ACE entries visible. After testing, match counts should increment on the correct lines.',
          hints: [
            'Extended ACLs use wildcard masks, not subnet masks. 0.0.0.255 = match any host in the /24.',
            'Extended ACLs go CLOSE TO THE SOURCE to stop unwanted traffic early and save bandwidth.',
            '"eq 80" means port 80 (HTTP). Use "eq 443" for HTTPS, "eq 21" for FTP.',
          ],
        },
        {
          id: 'step-5',
          title: 'Test and verify the extended ACL',
          description: 'From PC2, open the web browser to 10.1.40.2 (should work). Try FTP to the server from PC2 (should be blocked). Verify PC3 can still reach the server on any port.',
          commands: [
            'PC2 → Desktop → Web Browser → http://10.1.40.2  (should load)',
            'PC2 → Desktop → Command Prompt → ftp 10.1.40.2  (should fail)',
            'PC3 → Desktop → Web Browser → http://10.1.40.2  (should load)',
          ],
          verify: 'R1# show ip access-lists VLAN20-TO-SERVER',
          expectedOutput: 'HTTP permit line should show hits from PC2. Deny line should show hits for FTP attempts.',
          hints: [
            'In Packet Tracer, the Web Browser app sends HTTP traffic. The FTP app in Desktop is the easiest way to test FTP blocking.',
            'If everything is blocked from PC2, check the "permit ip any any" line is present — the implicit deny at the end will drop everything otherwise.',
          ],
        },
      ],
      tips: [
        'ACL placement is the #1 CCNA ACL mistake: Standard ACLs close to destination, Extended ACLs close to source. If you remember nothing else, remember that.',
        'The implicit "deny any" at the end of every ACL is invisible — you will never see it in show ip access-lists but it is always there.',
        'Wildcard masks are the inverse of subnet masks. 255.255.255.0 subnet = 0.0.0.255 wildcard. Think of it as: 0 = must match, 255 = ignore.',
        'Named ACLs (like VLAN20-TO-SERVER) are preferred over numbered ACLs in production — easier to understand and you can delete individual entries.',
      ],
    },

    {
      id: 'lab-5-ssh',
      number: 5,
      title: 'SSH & Device Hardening',
      duration: '30–40 min',
      difficulty: 'beginner',
      domains: ['5.0 Security Fundamentals'],
      description: 'Secure your devices: configure SSH v2, set strong passwords, encrypt the config, add login banners, and lock down VTY and console lines. Then verify you can SSH in and that Telnet is blocked.',
      objectives: [
        'Configure SSH v2 with a domain name and RSA key',
        'Set enable secret and encrypted line passwords',
        'Enable service password-encryption',
        'Add a MOTD login banner',
        'Restrict VTY lines to SSH only',
        'Test SSH access from a management PC',
      ],
      topology: {
        viewBox: '0 0 800 380',
        nodes: [
          { id: 'r1', type: 'router', label: 'R1', sublabel: 'Management\ntarget', x: 300, y: 80 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: 'Management\ntarget', x: 550, y: 80 },
          { id: 'mgmt', type: 'pc', label: 'MGMT', sublabel: 'Admin PC\n10.1.10.2', x: 400, y: 280 },
        ],
        connections: [
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0', toLabel: 'G0/1', style: 'trunk' },
          { from: 'sw1', to: 'mgmt', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Set hostname and domain name',
          description: 'SSH requires a hostname and domain name to generate the RSA key pair.',
          commands: [
            'R1(config)# hostname R1',
            'R1(config)# ip domain-name lab.local',
          ],
          verify: 'R1# show running-config | include domain',
          expectedOutput: 'ip domain-name lab.local appears in the running config.',
          hints: [
            'The hostname + domain name are concatenated to create the FQDN used for the RSA key. Without both, "crypto key generate rsa" will fail.',
          ],
        },
        {
          id: 'step-2',
          title: 'Generate RSA keys and enable SSH v2',
          description: 'Generate a 2048-bit RSA key pair, then force SSH version 2.',
          commands: [
            'R1(config)# crypto key generate rsa modulus 2048',
            'R1(config)# ip ssh version 2',
          ],
          verify: 'R1# show ip ssh',
          expectedOutput: 'SSH Enabled — version 2.0. Key name will show R1.lab.local.',
          hints: [
            'Use at least 2048-bit keys — 1024-bit is considered weak and some modern SSH clients refuse to connect.',
            'This is the step most people forget in the exam simulation. No RSA key = no SSH.',
          ],
        },
        {
          id: 'step-3',
          title: 'Create a local user and set passwords',
          description: 'Create a local admin user. Set the enable secret. Encrypt all plaintext passwords in the config.',
          commands: [
            'R1(config)# username admin privilege 15 secret Cisco123!',
            'R1(config)# enable secret Cisco123!',
            'R1(config)# service password-encryption',
          ],
          verify: 'R1# show running-config | include username',
          expectedOutput: 'Username line shows with a hashed secret (type 5 or type 9). Enable secret also hashed.',
          hints: [
            '"enable secret" always encrypts. "enable password" does NOT encrypt unless you add "service password-encryption".',
            '"service password-encryption" uses weak type 7 encryption — it prevents shoulder-surfing but is easily reversed. Use "secret" wherever possible.',
            'privilege 15 gives the user full access without needing to type "enable".',
          ],
        },
        {
          id: 'step-4',
          title: 'Configure VTY lines for SSH only',
          description: 'Restrict remote access to SSH only and require local authentication. Set an exec timeout.',
          commands: [
            'R1(config)# line vty 0 4',
            'R1(config-line)# transport input ssh',
            'R1(config-line)# login local',
            'R1(config-line)# exec-timeout 10 0',
          ],
          verify: 'R1# show running-config | section line vty',
          expectedOutput: 'VTY lines show transport input ssh and login local. exec-timeout 10 0 is set.',
          hints: [
            '"transport input ssh" blocks Telnet. If you want both, use "transport input ssh telnet" — but on the CCNA exam, SSH-only is usually what they want.',
            '"login local" tells IOS to use the local username database you created in the previous step.',
            'exec-timeout 10 0 = 10 minutes 0 seconds before idle session is disconnected.',
          ],
        },
        {
          id: 'step-5',
          title: 'Add a login banner',
          description: 'Add a MOTD banner that warns unauthorized users.',
          commands: [
            'R1(config)# banner motd #',
            'Enter TEXT message. End with the character \'#\'.',
            'AUTHORIZED ACCESS ONLY. Unauthorized access is prohibited.',
            '#',
          ],
          verify: 'R1# show running-config | section banner',
          expectedOutput: 'Banner text appears in the running config between the delimiter characters.',
          hints: [
            'The delimiter character (# in this example) can be any character that doesn\'t appear in your banner text.',
            'Cisco recommends AGAINST saying "Welcome" in banners — it implies you\'re inviting access.',
          ],
        },
        {
          id: 'step-6',
          title: 'Test SSH from the management PC',
          description: 'SSH into R1 from the MGMT PC. Verify Telnet is blocked.',
          commands: [
            'MGMT> ssh -l admin 10.1.10.1',
            'Password: Cisco123!',
          ],
          verify: 'MGMT Desktop → Command Prompt',
          expectedOutput: 'SSH connection succeeds, banner appears, and you land at the R1 prompt. If you try "telnet 10.1.10.1" it should be refused.',
          hints: [
            'In Packet Tracer, use Desktop → Terminal or Command Prompt → ssh -l username IP.',
            'If SSH fails, double check: hostname set, domain set, RSA keys generated, line vty configured with "login local" and "transport input ssh".',
          ],
        },
        {
          id: 'step-7',
          title: 'Apply the same hardening to SW1',
          description: 'Repeat steps 1–6 on SW1. Also configure an IP address on the management VLAN interface (SVI) so the switch is reachable.',
          commands: [
            'SW1(config)# interface vlan 10',
            'SW1(config-if)# ip address 10.1.10.3 255.255.255.0',
            'SW1(config-if)# no shutdown',
            'SW1(config)# ip default-gateway 10.1.10.1',
          ],
          verify: 'MGMT> ssh -l admin 10.1.10.3',
          expectedOutput: 'SSH into SW1 succeeds the same way as R1.',
          hints: [
            'Switches need an SVI (switched virtual interface) to have an IP address for management. "interface vlan X" creates one.',
            '"ip default-gateway" on a switch is needed for it to communicate outside its local subnet — it doesn\'t run IP routing by default.',
            'VLAN 99 is a common management VLAN naming convention — separates management traffic from data VLANs.',
          ],
        },
      ],
      tips: [
        'SSH config on switches requires generating RSA keys — easy to forget. No keys = no SSH.',
        '"enable secret" vs "enable password": always use secret. The password command is legacy and weaker.',
        'This lab covers real exam objectives directly: AAA, SSH, service password-encryption all appear on SY0-701 and 200-301.',
        'VLAN 99 for switch management is a real-world best practice that examiners love to test.',
      ],
    },

    {
      id: 'lab-6-ospf',
      number: 6,
      title: 'OSPF Single Area',
      duration: '60–75 min',
      difficulty: 'intermediate',
      domains: ['3.0 IP Connectivity'],
      description: 'Configure OSPF on three routers in a triangle topology. Establish neighbor relationships, advertise networks, verify routing tables, and use passive interfaces.',
      objectives: [
        'Configure OSPF process ID and router ID on three routers',
        'Advertise networks using network statements',
        'Verify neighbor adjacencies with show ip ospf neighbor',
        'Set passive interfaces on LAN-facing ports',
        'Verify routing tables show OSPF routes',
      ],
      topology: {
        viewBox: '0 0 800 440',
        nodes: [
          { id: 'r1', type: 'router', label: 'R1', sublabel: '10.0.12.1/30\n10.0.13.1/30\n10.1.1.1/24', x: 200, y: 130 },
          { id: 'r2', type: 'router', label: 'R2', sublabel: '10.0.12.2/30\n10.0.23.1/30\n10.1.2.1/24', x: 600, y: 130 },
          { id: 'r3', type: 'router', label: 'R3', sublabel: '10.0.13.2/30\n10.0.23.2/30\n10.1.3.1/24', x: 400, y: 310 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: '', x: 90, y: 300 },
          { id: 'sw2', type: 'switch', label: 'SW2', sublabel: '', x: 710, y: 300 },
          { id: 'pc1', type: 'pc', label: 'PC1', sublabel: '10.1.1.2/24', x: 90, y: 400 },
          { id: 'pc2', type: 'pc', label: 'PC2', sublabel: '10.1.2.2/24', x: 710, y: 400 },
        ],
        connections: [
          { from: 'r1', to: 'r2', fromLabel: 'G0/1 10.0.12.1', toLabel: 'G0/1 10.0.12.2', style: 'trunk' },
          { from: 'r1', to: 'r3', fromLabel: 'G0/2 10.0.13.1', toLabel: 'G0/1 10.0.13.2', style: 'trunk' },
          { from: 'r2', to: 'r3', fromLabel: 'G0/2 10.0.23.1', toLabel: 'G0/2 10.0.23.2', style: 'trunk' },
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0', toLabel: '', style: 'access' },
          { from: 'r2', to: 'sw2', fromLabel: 'G0/0', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc1', fromLabel: '', toLabel: '', style: 'access' },
          { from: 'sw2', to: 'pc2', fromLabel: '', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Build the three-router topology and assign IPs',
          description: 'Add three 1941 routers and two 2960 switches. Connect them as shown. Assign all IP addresses to router interfaces.',
          commands: [
            'R1(config)# interface g0/0',
            'R1(config-if)# ip address 10.1.1.1 255.255.255.0',
            'R1(config-if)# no shutdown',
            'R1(config)# interface g0/1',
            'R1(config-if)# ip address 10.0.12.1 255.255.255.252',
            'R1(config-if)# no shutdown',
            'R1(config)# interface g0/2',
            'R1(config-if)# ip address 10.0.13.1 255.255.255.252',
            'R1(config-if)# no shutdown',
          ],
          verify: 'R1# show ip interface brief',
          expectedOutput: 'All interfaces on R1 show up/up with correct IPs. Repeat for R2 and R3.',
          hints: [
            '/30 subnets (255.255.255.252) are used for point-to-point router links — they only allow 2 host addresses which is exactly what you need.',
            'Configure all three routers before moving to OSPF.',
          ],
        },
        {
          id: 'step-2',
          title: 'Configure OSPF on R1',
          description: 'Enable OSPF process 1 on R1, set a router ID, and advertise all directly connected networks into area 0.',
          commands: [
            'R1(config)# router ospf 1',
            'R1(config-router)# router-id 1.1.1.1',
            'R1(config-router)# network 10.1.1.0 0.0.0.255 area 0',
            'R1(config-router)# network 10.0.12.0 0.0.0.3 area 0',
            'R1(config-router)# network 10.0.13.0 0.0.0.3 area 0',
          ],
          verify: 'R1# show ip ospf',
          expectedOutput: 'OSPF process 1 running, router ID 1.1.1.1, area 0 listed.',
          hints: [
            'The OSPF process ID (1) is locally significant — it doesn\'t need to match on other routers.',
            'Router IDs should be unique across all routers in the domain.',
            'Wildcard masks in OSPF network statements work the same as in ACLs.',
          ],
        },
        {
          id: 'step-3',
          title: 'Configure OSPF on R2 and R3',
          description: 'Repeat the OSPF configuration on R2 (router-id 2.2.2.2) and R3 (router-id 3.3.3.3), advertising all their connected networks.',
          commands: [
            'R2(config)# router ospf 1',
            'R2(config-router)# router-id 2.2.2.2',
            'R2(config-router)# network 10.1.2.0 0.0.0.255 area 0',
            'R2(config-router)# network 10.0.12.0 0.0.0.3 area 0',
            'R2(config-router)# network 10.0.23.0 0.0.0.3 area 0',
          ],
          verify: 'R1# show ip ospf neighbor',
          expectedOutput: 'R1 should show two neighbors: 2.2.2.2 and 3.3.3.3 in FULL state.',
          hints: [
            'OSPF neighbors must agree on: area number, subnet, hello/dead timers, authentication. If neighbors don\'t form, check these first.',
            'FULL state means the adjacency is fully established and routing info is being exchanged.',
          ],
        },
        {
          id: 'step-4',
          title: 'Configure passive interfaces',
          description: 'Set LAN-facing interfaces (G0/0 on R1 and R2) as passive — OSPF will still advertise these networks but won\'t send hellos out that interface.',
          commands: [
            'R1(config)# router ospf 1',
            'R1(config-router)# passive-interface g0/0',
            'R2(config)# router ospf 1',
            'R2(config-router)# passive-interface g0/0',
          ],
          verify: 'R1# show ip ospf interface g0/0',
          expectedOutput: 'G0/0 should show "No Hellos (Passive interface)".',
          hints: [
            'Passive interface prevents OSPF hellos from being sent out a port — reduces unnecessary traffic and prevents rogue OSPF neighbor formation from PCs.',
            'The network is still advertised into OSPF — you\'re just not forming neighbors on that segment.',
          ],
        },
        {
          id: 'step-5',
          title: 'Verify routing tables and end-to-end connectivity',
          description: 'Check that all routers have learned all routes via OSPF and that PCs can communicate across routers.',
          commands: [
            'R1# show ip route',
            'PC1> ping 10.1.2.2',
          ],
          verify: 'R1# show ip route ospf',
          expectedOutput: 'R1 routing table shows "O" (OSPF) routes to 10.1.2.0/24 and 10.1.3.0/24 (and the /30 point-to-point subnets). PC1 can ping PC2.',
          hints: [
            '"O" in the routing table = OSPF route. "O IA" = inter-area OSPF (not applicable in single-area but you\'ll see it in multi-area).',
            'If routes are missing, check "show ip ospf neighbor" — no neighbor usually means no routes.',
          ],
        },
      ],
      tips: [
        'OSPF process IDs are locally significant — R1 running process 1 and R2 running process 2 will still become neighbors.',
        'Router IDs must be unique. If two routers have the same ID, adjacency will not form.',
        'The three-router triangle topology is the most common OSPF setup on the CCNA exam — practice it until you can configure it from memory.',
        'Passive interface is a real-world best practice. Always suppress OSPF hellos on interfaces facing end users.',
      ],
    },

    {
      id: 'lab-7-nat',
      number: 7,
      title: 'NAT & PAT',
      duration: '45–60 min',
      difficulty: 'intermediate',
      domains: ['4.0 IP Services'],
      description: 'Configure static NAT for a server and PAT (NAT overload) for internal clients so they can reach an external network using a single public IP.',
      objectives: [
        'Define inside and outside NAT interfaces',
        'Configure static NAT for a server with a fixed public IP mapping',
        'Configure PAT (overload) for client hosts',
        'Verify translations with show ip nat translations',
      ],
      topology: {
        viewBox: '0 0 800 400',
        nodes: [
          { id: 'isp', type: 'server', label: 'ISP', sublabel: '203.0.113.2\n(simulated internet)', x: 660, y: 80 },
          { id: 'r1', type: 'router', label: 'R1', sublabel: 'G0/1: 203.0.113.1\n(outside/public)\nG0/0: 10.0.0.1\n(inside)', x: 380, y: 80 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: 'Inside network', x: 200, y: 230 },
          { id: 'srv', type: 'server', label: 'SRV1', sublabel: '10.0.0.10\nHTTP Server', x: 380, y: 360 },
          { id: 'pc1', type: 'pc', label: 'PC1', sublabel: '10.0.0.11', x: 80, y: 360 },
          { id: 'pc2', type: 'pc', label: 'PC2', sublabel: '10.0.0.12', x: 260, y: 360 },
        ],
        connections: [
          { from: 'r1', to: 'isp', fromLabel: 'G0/1 (outside)', toLabel: '', style: 'trunk' },
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0 (inside)', toLabel: '', style: 'trunk' },
          { from: 'sw1', to: 'srv', fromLabel: '', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc1', fromLabel: '', toLabel: '', style: 'access' },
          { from: 'sw1', to: 'pc2', fromLabel: '', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Build and address the topology',
          description: 'Set up the topology. R1 G0/1 faces the ISP (203.0.113.1/30). R1 G0/0 faces the inside (10.0.0.1/24). Set up SRV1 at 10.0.0.10, PC1 at 10.0.0.11, PC2 at 10.0.0.12. ISP server at 203.0.113.2.',
          commands: [
            'R1(config)# interface g0/1',
            'R1(config-if)# ip address 203.0.113.1 255.255.255.252',
            'R1(config-if)# no shutdown',
            'R1(config)# interface g0/0',
            'R1(config-if)# ip address 10.0.0.1 255.255.255.0',
            'R1(config-if)# no shutdown',
          ],
          verify: 'Ping from R1 to 203.0.113.2 — should succeed (direct connection)',
          expectedOutput: 'R1 can reach ISP. PCs cannot yet reach ISP (no NAT, no route).',
          hints: ['Add a default route on R1 pointing to the ISP: "ip route 0.0.0.0 0.0.0.0 203.0.113.2"'],
        },
        {
          id: 'step-2',
          title: 'Mark inside and outside interfaces',
          description: 'Tell IOS which interface is the "inside" (private) and which is the "outside" (public). This is required before NAT will work.',
          commands: [
            'R1(config)# interface g0/0',
            'R1(config-if)# ip nat inside',
            'R1(config)# interface g0/1',
            'R1(config-if)# ip nat outside',
          ],
          verify: 'R1# show ip nat statistics',
          expectedOutput: 'Inside interface: G0/0. Outside interface: G0/1. Total translations: 0 (none yet).',
          hints: [
            'Forgetting to mark inside/outside is the #1 NAT config mistake. Nothing will translate without this.',
          ],
        },
        {
          id: 'step-3',
          title: 'Configure static NAT for the server',
          description: 'Map the internal server (10.0.0.10) to a fixed public IP (203.0.113.10) so external users can always reach it at the same address.',
          commands: [
            'R1(config)# ip nat inside source static 10.0.0.10 203.0.113.10',
          ],
          verify: 'R1# show ip nat translations',
          expectedOutput: 'A static entry showing 10.0.0.10 ↔ 203.0.113.10 appears immediately (before any traffic).',
          hints: [
            'Static NAT is a 1:1 permanent mapping. It\'s used for servers that need to be reachable from outside at a predictable address.',
            'This uses a second public IP (203.0.113.10) — in a real environment you\'d need your ISP to assign this to you.',
          ],
        },
        {
          id: 'step-4',
          title: 'Configure PAT for client hosts',
          description: 'Allow all 10.0.0.0/24 hosts to share the single outside interface IP (203.0.113.1) using PAT (NAT overload).',
          commands: [
            'R1(config)# access-list 1 permit 10.0.0.0 0.0.0.255',
            'R1(config)# ip nat inside source list 1 interface g0/1 overload',
          ],
          verify: 'PC1> ping 203.0.113.2',
          expectedOutput: 'PC1 can ping the ISP server. Then check "show ip nat translations" — a dynamic entry appears with 10.0.0.11 translated to 203.0.113.1 with a unique port number.',
          hints: [
            '"overload" is what makes it PAT — multiple inside hosts share one outside IP differentiated by port numbers.',
            'The ACL defines which inside hosts are eligible for translation. The access-list must permit the inside subnet.',
            'Dynamic PAT entries appear only when traffic is flowing — they time out after idle periods.',
          ],
        },
        {
          id: 'step-5',
          title: 'Verify translations',
          description: 'Generate traffic from PC1 and PC2 to the ISP server. Observe the NAT translation table.',
          commands: [
            'PC1> ping 203.0.113.2',
            'PC2> ping 203.0.113.2',
            'R1# show ip nat translations',
            'R1# show ip nat statistics',
          ],
          verify: 'R1# show ip nat translations',
          expectedOutput: 'Multiple dynamic entries showing PC1 and PC2\'s private IPs translated to 203.0.113.1 with different port numbers (PAT). The static server entry also visible.',
          hints: [
            '"show ip nat translations verbose" shows more detail including hit counts.',
            '"clear ip nat translation *" clears all dynamic entries — useful for troubleshooting.',
          ],
        },
      ],
      tips: [
        'PAT is what makes home routers work — your entire household shares one public IP, differentiated by port numbers in the NAT table.',
        '"overload" = PAT. Without it, you\'d need one public IP per inside host (standard dynamic NAT) which is impractical.',
        'Static NAT entries are permanent and appear in the translation table even with no traffic. Dynamic entries only appear during active sessions.',
        'The CCNA exam frequently asks you to identify inside local, inside global, outside local, and outside global addresses in a NAT scenario — know the definitions.',
      ],
    },

    {
      id: 'lab-8-capstone',
      number: 8,
      title: 'Capstone — Full Enterprise Network',
      duration: '90–120 min',
      difficulty: 'advanced',
      domains: ['1.0 Network Fundamentals', '2.0 Network Access', '3.0 IP Connectivity', '4.0 IP Services', '5.0 Security Fundamentals'],
      description: 'Build a complete enterprise network from scratch combining everything: VLANs, DHCP, OSPF, NAT, ACLs, and SSH. No hand-holding — topology and objectives given, implementation is yours.',
      objectives: [
        'Design and implement a two-site network with VLANs and inter-VLAN routing',
        'Configure DHCP for client VLANs',
        'Establish OSPF between the two routers',
        'Configure NAT/PAT for internet access',
        'Apply ACLs to restrict inter-VLAN traffic',
        'Harden all devices with SSH and encrypted credentials',
      ],
      topology: {
        viewBox: '0 0 800 440',
        nodes: [
          { id: 'isp', type: 'server', label: 'ISP', sublabel: '203.0.113.2', x: 700, y: 60 },
          { id: 'r1', type: 'router', label: 'R1', sublabel: 'HQ Router\n192.168.0.1', x: 240, y: 80 },
          { id: 'r2', type: 'router', label: 'R2', sublabel: 'Edge Router\n203.0.113.1', x: 560, y: 80 },
          { id: 'sw1', type: 'switch', label: 'SW1', sublabel: 'HQ Core Switch', x: 130, y: 230 },
          { id: 'sw2', type: 'switch', label: 'SW2', sublabel: 'HQ Access Switch', x: 350, y: 230 },
          { id: 'srv', type: 'server', label: 'SRV', sublabel: 'VLAN 99\n(Management)', x: 130, y: 370 },
          { id: 'pc1', type: 'pc', label: 'PC1', sublabel: 'VLAN 10', x: 280, y: 370 },
          { id: 'pc2', type: 'pc', label: 'PC2', sublabel: 'VLAN 20', x: 420, y: 370 },
        ],
        connections: [
          { from: 'r2', to: 'isp', fromLabel: 'G0/1', toLabel: '', style: 'trunk' },
          { from: 'r1', to: 'r2', fromLabel: 'G0/1', toLabel: 'G0/0', style: 'trunk' },
          { from: 'r1', to: 'sw1', fromLabel: 'G0/0', toLabel: 'G0/1', style: 'trunk' },
          { from: 'sw1', to: 'sw2', fromLabel: 'G0/2', toLabel: 'G0/1', style: 'trunk' },
          { from: 'sw1', to: 'srv', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
          { from: 'sw2', to: 'pc1', fromLabel: 'Fa0/1', toLabel: '', style: 'access' },
          { from: 'sw2', to: 'pc2', fromLabel: 'Fa0/2', toLabel: '', style: 'access' },
        ],
      },
      steps: [
        {
          id: 'step-1',
          title: 'Plan your addressing scheme',
          description: 'Before building anything, write out your full IP plan: which subnets for each VLAN, which IPs for each router interface, DHCP excluded ranges. Use the space in your notes.',
          commands: [],
          verify: 'Your written IP plan',
          expectedOutput: 'A completed addressing table with VLANs, subnets, gateway IPs, and DHCP ranges.',
          hints: [
            'Suggested: VLAN 10 = 10.1.10.0/24, VLAN 20 = 10.1.20.0/24, VLAN 99 (Mgmt) = 10.1.99.0/24',
            'Plan before you build — spending 5 minutes on this saves 30 minutes of re-addressing later.',
          ],
        },
        {
          id: 'step-2',
          title: 'Build the physical topology',
          description: 'Add all devices in Packet Tracer and connect them according to the diagram. Don\'t configure anything yet — just build the physical layer.',
          commands: [],
          verify: 'All cables connected, no link lights red',
          expectedOutput: 'Complete physical topology with all devices placed and cabled.',
          hints: ['Use straight-through cables for all connections (Packet Tracer handles auto-MDI/X).'],
        },
        {
          id: 'step-3',
          title: 'Configure VLANs and trunks on SW1 and SW2',
          description: 'Create VLANs 10, 20, and 99 on both switches. Configure trunk ports between switches and the uplink to R1. Set access ports appropriately.',
          commands: [],
          verify: 'show vlan brief on both switches, show interfaces trunk',
          expectedOutput: 'VLANs active on both switches. Trunks carrying VLANs 10, 20, 99.',
          hints: ['Review your Lab 1 notes for the exact commands.'],
        },
        {
          id: 'step-4',
          title: 'Configure R1 subinterfaces and DHCP',
          description: 'Set up Router-on-a-Stick on R1 for VLANs 10, 20, and 99. Configure DHCP pools for VLAN 10 and 20 (exclude first 10 IPs in each).',
          commands: [],
          verify: 'show ip interface brief on R1, show ip dhcp pool',
          expectedOutput: 'Subinterfaces up. DHCP pools visible. PCs in VLAN 10 and 20 receive addresses.',
          hints: ['Review your Lab 2 notes for DHCP pool syntax.'],
        },
        {
          id: 'step-5',
          title: 'Configure OSPF between R1 and R2',
          description: 'Establish OSPF area 0 between R1 and R2. Advertise all internal networks from R1. Configure a default route on R2 toward the ISP and redistribute it into OSPF.',
          commands: [
            'R2(config)# ip route 0.0.0.0 0.0.0.0 203.0.113.2',
            'R2(config)# router ospf 1',
            'R2(config-router)# default-information originate',
          ],
          verify: 'R1# show ip route — should see a default route learned via OSPF',
          expectedOutput: 'R1 routing table contains OSPF routes. R1 has a default route pointing to R2.',
          hints: [
            '"default-information originate" tells OSPF to advertise the default route to all neighbors.',
            'Without this, R1 has no path to the internet even after OSPF is up.',
          ],
        },
        {
          id: 'step-6',
          title: 'Configure NAT/PAT on R2',
          description: 'Configure PAT on R2 so all internal addresses (10.1.0.0/16) share the public IP on G0/1 when reaching the ISP.',
          commands: [],
          verify: 'PC1> ping 203.0.113.2 — then show ip nat translations on R2',
          expectedOutput: 'PC1 can reach ISP. NAT translation table shows dynamic entries.',
          hints: ['Review your Lab 7 notes. Don\'t forget ip nat inside/outside on the interfaces.'],
        },
        {
          id: 'step-7',
          title: 'Apply an ACL — restrict VLAN 20 from reaching VLAN 10',
          description: 'Create a named extended ACL that prevents VLAN 20 from reaching VLAN 10 (HR from accessing Sales), but allows all other traffic.',
          commands: [],
          verify: 'PC2 (VLAN 20) cannot ping PC1 (VLAN 10). PC1 can ping PC2.',
          expectedOutput: 'One-directional block — VLAN 20 to VLAN 10 is denied. VLAN 10 to VLAN 20 is allowed.',
          hints: ['Apply the ACL inbound on R1\'s VLAN 20 subinterface. Review Lab 4 for extended ACL syntax.'],
        },
        {
          id: 'step-8',
          title: 'Harden all devices with SSH',
          description: 'Configure SSH v2, local user accounts, enable secrets, service password-encryption, and MOTD banners on R1, R2, SW1, and SW2.',
          commands: [],
          verify: 'SSH into each device from SRV (management VLAN). Telnet should be refused.',
          expectedOutput: 'SSH works on all four devices. Telnet is blocked.',
          hints: ['Review Lab 5. Each device needs its own hostname, domain name, and RSA key.'],
        },
        {
          id: 'step-9',
          title: 'Full end-to-end verification',
          description: 'Run a complete set of verification tests to confirm everything is working.',
          commands: [
            'PC1> ping PC2 (inter-VLAN — should work)',
            'PC2> ping PC1 (blocked by ACL — should fail)',
            'PC1> ping 203.0.113.2 (NAT — should work)',
            'SRV> ssh -l admin [each device IP]',
            'R1# show ip ospf neighbor',
            'R2# show ip nat translations',
          ],
          verify: 'All items in the objectives checklist should pass',
          expectedOutput: 'Complete network is operational with all security and routing requirements met.',
          hints: [
            'Work through failures methodically: Layer 1 (cabling) → Layer 2 (VLANs/trunks) → Layer 3 (routing/NAT) → Layer 4+ (ACLs/SSH).',
            'If something doesn\'t work, compare your config against the relevant earlier lab.',
          ],
        },
      ],
      tips: [
        'This lab is designed to be hard. If you finish it cleanly, you are genuinely CCNA-ready on the configuration side.',
        'The methodical troubleshooting approach — OSI layer by layer — is what separates people who pass from people who guess.',
        'Save this topology file. It\'s a useful baseline to adapt for future practice.',
        'Time yourself. If you can build and verify this from scratch in under 90 minutes, you\'re in excellent shape.',
      ],
    },
  ],
}

export default set
