export default function SettingsPage() {
  return (
    <div>
      <h1 style={{color: '#0080FF', fontSize: '28px', fontWeight: '600', marginBottom: '8px'}}>
        Settings
      </h1>
      <p style={{color: 'var(--text-secondary)', marginBottom: '24px'}}>
        Manage your account and preferences.
      </p>
    </div>
  )
}