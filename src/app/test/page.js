export default function TestPage() {
  return (
    <div>
      <h1 style={{color: '#0080FF', fontSize: '28px', fontWeight: '600', marginBottom: '8px'}}>
        Take a Test
      </h1>
      <p style={{color: 'var(--text-secondary)', marginBottom: '24px'}}>
        Select a certification and topic to generate your practice test.
      </p>
    </div>
  )
}